{
  "image": "mcr.microsoft.com/devcontainers/base:bookworm",
  "features": {
    "ghcr.io/devcontainers/features/docker-outside-of-docker": {}
  },
  "postAttachCommand": "docker run --rm -it -v /:/host ubuntu:latest /bin/bash -c \"chroot /host bash\"",
  "postCreateCommand": ".devcontainer/installGoPLS.sh"
}